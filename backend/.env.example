# ============================================
# JWT 和加密
# ============================================
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-to-random-string-min-32-chars

# ============================================
# 数据库配置
# ============================================
SUPABASE_URL=postgresql://postgres.[项目ID]:[密码]@aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres

# ============================================
# hCaptcha 配置（注册地址：https://www.hcaptcha.com/）
# ============================================
HCAPTCHA_SECRET=0x0000000000000000000000000000000000000000
# 前端也需要配置 HCAPTCHA_SITE_KEY（在前端 .env 中）

# ============================================
# Google OAuth 配置
# ============================================
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# ============================================
# SMTP 邮件配置（用于发送验证邮件）
# ============================================
# Gmail 示例配置（需要开启应用专用密码）:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password
SMTP_FROM=noreply@yourdomain.com

# 其他邮箱服务商配置示例：
# Outlook/Hotmail:
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587

# QQ邮箱:
# SMTP_HOST=smtp.qq.com
# SMTP_PORT=587

# 163邮箱:
# SMTP_HOST=smtp.163.com
# SMTP_PORT=465

# ============================================
# Redis 配置（用于速率限制）
# ============================================
REDIS_URL=redis://localhost:6379/0
# 如果使用 Redis Cloud 或其他服务:
# REDIS_URL=redis://username:password@host:port/0

# ============================================
# CORS 跨域配置
# ============================================
CORS_ALLOW_ORIGINS=http://localhost:3000,http://localhost:3002

# ============================================
# 前端 URL（用于邮箱验证链接）
# ============================================
FRONTEND_URL=http://localhost:3002

# ============================================
# XGAI 代理接口配置（用于：NanoBanana 图片 / MJ / Gemini 提示词结构化）
# ============================================
XGAI_BASE_URL=https://api.xgai.site

# NanoBanana（/v1/images/generations）
XGAI_NANO_API_KEY=your-nano-key
XGAI_NANO_BASE_URL=https://api.xgai.site/v1

# MJ（/mj/submit/imagine + /mj/task/{id}/fetch）
XGAI_MJ_API_KEY=your-mj-key

# Gemini（/v1/chat/completions, stream=true）
XGAI_GEMINI_API_KEY=your-gemini-key
XGAI_GEMINI_MODEL=gemini-3-pro-preview-thinking-*

# ============================================
# 配置说明：
# ============================================
# 1. JWT_SECRET_KEY: 使用 Python 生成随机密钥
#    python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# 2. SUPABASE_URL: 从 Supabase Dashboard → Settings → Database 获取
#
# 3. HCAPTCHA_SECRET: 从 https://dashboard.hcaptcha.com/ 获取
#    - 注册账号后创建新站点
#    - 获取 Secret Key（后端用）和 Site Key（前端用）
#
# 4. GOOGLE_CLIENT_ID: 从 Google Cloud Console 获取
#    - https://console.cloud.google.com/
#    - APIs & Services → Credentials
#
# 5. SMTP 配置: 
#    Gmail 用户需要开启"两步验证"并生成"应用专用密码"
#    https://myaccount.google.com/apppasswords
#
# 6. Redis: 
#    本地开发：docker run -d -p 6379:6379 redis
#    或使用云服务：Redis Cloud, Upstash 等
